// Điền URL và publishable key lấy từ Supabase Dashboard.
// Publishable/anon key có thể đặt trong frontend khi đã bật RLS.
export const SUPABASE_CONFIG = {
  url: '',
  publishableKey: '',
  syncIntervalMs: 3000
};

